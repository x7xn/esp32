#
# This is a project Makefile. It is assumed the directory this Makefile resides in is a
# project subdirectory.
#

PROJECT_NAME := miio_app

CFLAGS := -D MIIO_LOG_LEVEL=1
CFLAGS += -D MIIO_AUTO_OTA_ENABLE=1
CFLAGS += -D MIIO_PROV_STATISTIC=1
CFLAGS += -D MIIO_KEY_EXCHANGE_ENABLE=1
export MIIO_KEY_EXCHANGE_ENABLE :=1

include $(IDF_PATH)/make/project.mk

